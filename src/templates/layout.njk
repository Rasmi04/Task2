<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}The Gilded Page{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    {% block head %}{% endblock %}
</head>
<body>
    <nav class="navbar navbar-expand-lg {% if darkNavbar %}navbar-dark{% else %}navbar-light{% endif %} sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="index.html" style="color: var(--deep-rose) !important;">The Gilded Page</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link {% if activePage == 'home' %}active{% endif %}" href="index.html" style="color: var(--deep-rose) !important;">Home</a></li>
                    <li class="nav-item"><a class="nav-link {% if activePage == 'collections' %}active{% endif %}" href="browse.html" style="color: var(--deep-rose) !important;">Collections</a></li>
                    <li class="nav-item"><a class="nav-link {% if activePage == 'request' %}active{% endif %}" href="request.html" style="color: var(--deep-rose) !important;">Request</a></li>
                </ul>
            </div>
        </div>
    </nav>

    {% block content %}{% endblock %}

    {% if showFooter %}
    <footer class="py-5 text-center mt-5" style="background: var(--soft-pink); border-top: 1px solid var(--rose-gold);">
        <p class="small text-uppercase mb-0 fw-bold">The Gilded Page © 2025</p>
    </footer>
    {% endif %}
</body>
</html>
